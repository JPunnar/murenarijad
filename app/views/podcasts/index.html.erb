<br>
<div class="container-fluid">
  <div class="row">
    <div class="col-md-12">
      <% if @podcasts.present? %>
        <br>
        <table class="table">
          <tr class="header-row" >
            <th>NR</th>
          </tr>
          <% @podcasts.each do |podcast| %>
            <tr>
              <td><strong><%= podcast.id %></strong></td>
            </tr>
          <% end %>
        </table>

      <% else %>
        <h5>Episoodid puuduvad.</h5>
      <% end %>
    </div>
  </div>
</div>
